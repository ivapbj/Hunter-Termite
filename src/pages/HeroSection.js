import React from "react";
import "./HeroSection.css"; // Make sure to import the CSS file

const HeroSection = () => {
  return (
    <div className="hero-section">
      <div className="Meet-pic"></div>
    </div>
  );
};

export default HeroSection;
